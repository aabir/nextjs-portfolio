
import styles from '../styles/utils.module.css'

const Uses = () => (
    //Need to add a header component with layout
    <div className={styles.container}>

        <p>This is page is under construction.</p>
    </div>
);

export default Uses;