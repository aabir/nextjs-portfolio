module.exports = {
    images: {
      domains: ['avatars2.githubusercontent.com'],
    },
  };

/** @type {import('next').NextConfig} */
const nextConfig = {
  output:'export',
};
export default nextConfig;